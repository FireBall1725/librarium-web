# Drives the Discord post in #releases.

project:
  name: Librarium Web
  emoji: "📚"
  footer: "Librarium · self-hosted book, manga, and comic tracker"

channels:
  stable:
    announce: true
    colour: "0x2ECC71"
    prefix: ""
  rc:
    announce: true
    colour: "0xF1C40F"
    prefix: "Release candidate — "
  nightly:
    announce: false

getting_started: |
  ```
  docker pull {{image}}
  ```
  The web client talks to `librarium-api`; both usually move together.

links:
  - name: Site
    url: https://librarium.press/
  - name: Source
    url: https://github.com/{{repo}}
  - name: Changes
    url: "{{compare_url}}"
