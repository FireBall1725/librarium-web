import XCTest

// Placeholder test so the LibrariumTests target compiles before the
// real cases are added. Removed in a follow-up commit on this branch.
final class PlaceholderTests: XCTestCase {
    func testItRuns() { XCTAssertTrue(true) }
}
